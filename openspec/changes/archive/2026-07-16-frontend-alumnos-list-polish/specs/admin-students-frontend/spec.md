# Delta: admin-students-frontend

See consolidated requirements in `../../spec.md` (REQ-SLIST-*, REQ-SEDIT-*).

## ADDED Requirements

Covered by REQ-SLIST-001…007 and REQ-SEDIT-001…004 in change root `spec.md`.

## MODIFIED Requirements

- Fuente privada / listado: allow `tieneEmail` and metric fields as nullable when API omits them; HTTP MUST NOT hardcode false/0 as truth.
- Estados: MUST include SVG icons and empty CTA to nuevo.
- Búsqueda/privacy unchanged (nombre + dniMostrar only).
